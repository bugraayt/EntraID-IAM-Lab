## What I did
Set up a Microsoft Entra ID tenant via a free Azure account to use as a sandbox environment for building and testing IAM configurations.

## Why it matters
A dedicated test tenant allows safe experimentation with identity and access management controls without risk to a production environment — standard practice before implementing changes in a real organization.