## Project Summary

Built a simulated IAM environment in Microsoft Entra ID for a small mock organization (5 users, 3 groups). Applied least-privilege principles by assigning role-appropriate permissions rather than department-based access, and verified security control enforcement (MFA/Security Defaults) via sign-in log analysis rather than relying on configuration screens alone.